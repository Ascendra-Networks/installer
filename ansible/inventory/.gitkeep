# This directory contains inventory for Ansible (Terraform-generated or manual).
# - hosts / hosts.example: host groups (masters, workers)
# - group_vars/: variables (copy all.yml.example → all.yml and edit)


