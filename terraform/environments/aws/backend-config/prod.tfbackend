# State path for workspace "prod". Path relative to cwd (terraform/environments/aws).
# Run: terraform init -reconfigure -backend-config=backend-config/prod.tfbackend

path = "../../../state/prod/state.tfstate"
